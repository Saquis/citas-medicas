import { ServiciosDirective } from './servicios.directive';

describe('ServiciosDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiciosDirective();
    expect(directive).toBeTruthy();
  });
});
